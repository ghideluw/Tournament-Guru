import React from 'react';
import { useQuery } from '@apollo/client';

import eventRegistration from '../components/eventRegistration';




const Home = () => {
  const { loading, data } = useQuery(<insert query here>);
  

  return ()
    
};

export default Home;
